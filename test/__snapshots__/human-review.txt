── 未选(进入时的样子)· w=96 ──
╭─ 人工终审 · 需要你判定 ─────────────────────────────────────────── quick · mission-mtll5d8t ─╮
│ 判据 · 冻结于进 DO 之前 ──────────────────────────────────────────────────────────────────── │
│   在新标签页实际验证:把一个小组件卡片和一个书签分组移到副屏后,通过指示点(ScreenDots 第二点亮 │
│   起)或 → 方向键切到副屏,副屏上能实际看到该卡片(标题栏+内容)和该书签分组(分组标题+链接图标)  │
│   渲染出来,而不是空白;同时任务答复中给出了导致副屏空白的具体代码级原因(定位到文件与逻辑)。   │
│                                                                                              │
│ 判定 ─────────────────────────────────────────────────────────────────────────────────────── │
│   通过 —— 收工                                                                               │
│   不通过 —— 我来说哪里不对                                                                   │
│                                                                                              │
│   两项都不预选:回车即过等于没看就按键,那不是外部判定                                         │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
 ↑↓ 选   Enter 确认   Esc 取消 = 本轮无结论(不是通过)

── 选中通过 · w=96 ──
╭─ 人工终审 · 需要你判定 ─────────────────────────────────────────── quick · mission-mtll5d8t ─╮
│ 判据 · 冻结于进 DO 之前 ──────────────────────────────────────────────────────────────────── │
│   在新标签页实际验证:把一个小组件卡片和一个书签分组移到副屏后,通过指示点(ScreenDots 第二点亮 │
│   起)或 → 方向键切到副屏,副屏上能实际看到该卡片(标题栏+内容)和该书签分组(分组标题+链接图标)  │
│   渲染出来,而不是空白;同时任务答复中给出了导致副屏空白的具体代码级原因(定位到文件与逻辑)。   │
│                                                                                              │
│ 判定 ─────────────────────────────────────────────────────────────────────────────────────── │
│ ▸ 通过 —— 收工                                                                               │
│   不通过 —— 我来说哪里不对                                                                   │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
 ↑↓ 选   Enter 确认   Esc 取消 = 本轮无结论(不是通过)

── 选中不通过 · w=96 ──
╭─ 人工终审 · 需要你判定 ─────────────────────────────────────────── quick · mission-mtll5d8t ─╮
│ 判据 · 冻结于进 DO 之前 ──────────────────────────────────────────────────────────────────── │
│   在新标签页实际验证:把一个小组件卡片和一个书签分组移到副屏后,通过指示点(ScreenDots 第二点亮 │
│   起)或 → 方向键切到副屏,副屏上能实际看到该卡片(标题栏+内容)和该书签分组(分组标题+链接图标)  │
│   渲染出来,而不是空白;同时任务答复中给出了导致副屏空白的具体代码级原因(定位到文件与逻辑)。   │
│                                                                                              │
│ 判定 ─────────────────────────────────────────────────────────────────────────────────────── │
│   通过 —— 收工                                                                               │
│ ▸ 不通过 —— 我来说哪里不对                                                                   │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
 ↑↓ 选   Enter 确认   Esc 取消 = 本轮无结论(不是通过)

── 写理由 · w=96 ──
╭─ 人工终审 · 需要你判定 ─────────────────────────────────────────── quick · mission-mtll5d8t ─╮
│ 判据 · 冻结于进 DO 之前 ──────────────────────────────────────────────────────────────────── │
│   在新标签页实际验证:把一个小组件卡片和一个书签分组移到副屏后,通过指示点(ScreenDots 第二点亮 │
│   起)或 → 方向键切到副屏,副屏上能实际看到该卡片(标题栏+内容)和该书签分组(分组标题+链接图标)  │
│   渲染出来,而不是空白;同时任务答复中给出了导致副屏空白的具体代码级原因(定位到文件与逻辑)。   │
│                                                                                              │
│ 哪里不对? ────────────────────────────────────────────────────────────────────────────────── │
│   副屏切过去还是空的,ScreenDots 第二点也没亮▁                                                │
│                                                                                              │
│   会写进 LOG,并作为下一轮 ACT 的输入                                                         │
╰──────────────────────────────────────────────────────────────────────────────────────────────╯
 Enter 提交理由   Esc 回上一步

── 窄终端 · 未选 · w=56 ──
╭─ 人工终审 · 需要你判定 ─── quick · mission-mtll5d8t ─╮
│ 判据 · 冻结于进 DO 之前 ──────────────────────────── │
│   在新标签页实际验证:把一个小组件卡片和一个书签分组  │
│   移到副屏后,通过指示点(ScreenDots 第二点亮起)或 →   │
│   方向键切到副屏,副屏上能实际看到该卡片(标题栏+内容) │
│   和该书签分组(分组标题+链接图标)渲染出来,而不是空白 │
│   ;同时任务答复中给出了导致副屏空白的具体代码级原因( │
│   定位到文件与逻辑)。                                │
│                                                      │
│ 判定 ─────────────────────────────────────────────── │
│   通过 —— 收工                                       │
│   不通过 —— 我来说哪里不对                           │
│                                                      │
│   两项都不预选:回车即过等于没看就按键,那不是外部判定 │
╰──────────────────────────────────────────────────────╯
 ↑↓ 选   Enter 确认   Esc 取消 = 本轮无结论(不是通过)

── 窄终端 · 写理由 · w=56 ──
╭─ 人工终审 · 需要你判定 ─── quick · mission-mtll5d8t ─╮
│ 判据 · 冻结于进 DO 之前 ──────────────────────────── │
│   在新标签页实际验证:把一个小组件卡片和一个书签分组  │
│   移到副屏后,通过指示点(ScreenDots 第二点亮起)或 →   │
│   方向键切到副屏,副屏上能实际看到该卡片(标题栏+内容) │
│   和该书签分组(分组标题+链接图标)渲染出来,而不是空白 │
│   ;同时任务答复中给出了导致副屏空白的具体代码级原因( │
│   定位到文件与逻辑)。                                │
│                                                      │
│ 哪里不对? ────────────────────────────────────────── │
│   还是空白▁                                          │
│                                                      │
│   会写进 LOG,并作为下一轮 ACT 的输入                 │
╰──────────────────────────────────────────────────────╯
 Enter 提交理由   Esc 回上一步
