# T1 探针结论:分层清单与素材核对

按 CLAUDE.md 分层清单通读 README.md、docs/ARCHITECTURE.md、仓库树,核对结果:

## 实际目录结构(与 CLAUDE.md 分层一致,另有补充)

- `src/index.ts` 装配层;另有 `src/tools.ts`(五个 LLM 工具)、`src/commands.ts`(/missions + /mission)、`src/check-runner.ts`、`src/briefs.ts` —— CLAUDE.md 分层清单没逐条列,README「代码结构」有
- `src/core/`:machine/breaker/verdict/baseline/tier/define/coverage/review/spike/criterion/types + `__tests__/`(10 个纯函数测试文件);CLAUDE.md 提到 machine/breaker/verdict/baseline/tier/define/spike/coverage/review,缺 criterion.ts —— 报告按实际文件写
- `src/store/`:repository/mission/check/evidence/log/git/scaffold/config/io/paths(README 概括为 v2 Repository 一类)
- `src/roles/`:models.ts + verifier.ts;`src/hooks/`:gate.ts + diagnostics.ts
- `src/ui/`:chrome/panel/plan-review/ask-review/status-view/dashboard/models-page/task-detail/renderer/tier-indicator
- `templates/`:missions-README.md、phases/{define,plan,do,check,act}.md、scripts/env-fingerprint.sh;`skills/pi-missions/`
- `docs/`:ARCHITECTURE.md(权威)+ 两份 research 稿 + PLAN-REVIEW-AND-DEFINE.md
- `test/`:15 个测试文件(runtime.smoke、UI 渲染矩阵、theme-colors 等)

## 未发现 README/CLAUDE 过时陈述

CLAUDE.md 的命令清单(npm test / node --test 单文件 / --test-name-pattern / tsc --noEmit / pi -e 装载)、README 的设计不变量 I1–I9、ARCHITECTURE.md 的术语与不变量落点均与代码树一致。

## 报告可引用的权威依据

- 定位与双层循环:README.md 开头
- 分层与硬约束:CLAUDE.md + ARCHITECTURE.md §2、§7
- 模块地图:README「代码结构」+ ARCHITECTURE.md §2
- 相位状态机:ARCHITECTURE.md §3
- 三档/证据分级/DEFINE/冻结/换脑:README 各节 + ARCHITECTURE.md §4
- 测试分工:CLAUDE.md「测试的分工」+ README「测试」

## 报告结构建议(面向新成员,概览级)

1. 项目定位 2. 双层循环工作流 3. 模块地图(src 分层 + templates/skills/docs)
4. 开发与验证(命令) 5. 硬约束摘要(I1–I9 + core 纯净)
